spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/argus
    username: argus
    password: argus
    hikari:
      pool-name: argus-pool

management:
  endpoint:
    health:
      show-details: always

logging:
  level:
    io.github.rafaeljc.argus: DEBUG
