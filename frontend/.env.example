# Build-time Vite substitution, baked into the bundle — not read at runtime. `/api/v1` is correct
# for local dev through the vite.config.ts proxy; a production build must be given the absolute
# API origin instead, since the SPA and API are served from different hosts.
VITE_API_BASE_URL=/api/v1
